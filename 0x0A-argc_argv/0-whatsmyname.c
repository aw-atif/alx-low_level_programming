#include <stdio.h>
#include <stdlib.h>

/**
 * That program print his name
 * argc: number of arguments
 * argv: array of arguments
 * Return 0 if it success
 * */
int main (int argc, char *argv[]){
    printf("%s\n", argv[0]);
    return 0;
}
